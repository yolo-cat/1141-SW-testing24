package bmi.cucumber;

import io.cucumber.junit.platform.engine.Cucumber;

/**
 * The RunCucumberTest class with the @Cucumber annotation serves as the entry point
 * for executing Cucumber tests when using JUnit 5. While the class appears to be "empty,"
 * it has a critical role in enabling Cucumber to work seamlessly with JUnit 5.
 */

@Cucumber
public class RunCucumberTest {
}

